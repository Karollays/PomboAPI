-- Criando a tabela evento
CREATE TABLE T_EVENTO (
    ID_EVENTO NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    ID_EMAIL NUMBER,
    DESCRICAO VARCHAR2(255),
    CONSTRAINT FK_EMAIL_EVENTO FOREIGN KEY (ID_EMAIL) REFERENCES T_EMAIL(ID_EMAIL)
);